#ifndef _MAIN_Hi_
#define _MAIN_H_

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>
#include <stdio.h>

int _addchar(char c);
int _printf(const char *format, ...);
int puts_s(char *c);

#endif
